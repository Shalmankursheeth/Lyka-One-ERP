services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: lyka
      POSTGRES_PASSWORD: lyka
      POSTGRES_DB: lyka
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lyka -d lyka"]
      interval: 2s
      timeout: 5s
      retries: 20
